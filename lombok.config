# 루트에서만 설정을 찾는다.
config.stop.bubbling = true

# @RequiredArgsConstructor가 만드는 생성자에 주입 애노테이션을 함께 옮긴다.
# 옮기지 않으면 같은 타입 빈이 둘 이상일 때 필드 이름에 의존해 주입되고, 이름을 바꾸는 순간
# NoUniqueBeanDefinitionException으로 기동이 죽는다.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
