-- 연결 시각을 비워 둘 수 있게 한다.
--
-- 회원 행은 OAuth 콜백 처리 도중에 만들어지지만, 그때는 아직 연결이 아니다. 토큰까지 저장한
-- 트랜잭션이 연결을 확정하고 이 값을 찍는다. 행 생성 시점에 현재 시각을 박아 두면, 연결이
-- 끝내 실패한 회원도 "그때 연결됨"으로 남아 기록이 거짓이 된다.
ALTER TABLE users
    ALTER COLUMN connected_at DROP NOT NULL;
